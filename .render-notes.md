# Render deployment notes

- App uses SQLite.
- `DATABASE_PATH` is set to `./data/users.db` by default in `render.yaml`.
- On Render free plan, filesystem is ephemeral. Data may reset on redeploy/restart.
- For persistent production data, move to PostgreSQL (recommended) or attach persistent disk on a supported plan.
